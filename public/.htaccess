<IfModule mod_rewrite.c>
  RewriteEngine On
  # Rewrite requests to /page to /page.html
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteRule ^([^\.]+)$ $1.html [NC,L]
</IfModule>
